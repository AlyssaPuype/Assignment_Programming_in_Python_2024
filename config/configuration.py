"""DATABASE CONSTANTS"""
DATABASE_NAME = "StudyTracker.db"
DATABASE_PATH ="/path/to/database"

"""SESSION TABLE"""
STATUS_VALUES = ("td", "ip" , "d") #not used, I wanted to make these constants so its easier to use them in my code, but haven't got to this point)
"""
td = to do
ip = in progress
d  = done
"""